import { useContext } from "react";
import { AppContext } from "./AppContext";

export default function Settings(){
 

    return ( 
        <main>
            <h1>Settings</h1>
        </main>
        )
}